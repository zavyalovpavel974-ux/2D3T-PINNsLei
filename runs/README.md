# Runs

PINNs training logs, copied outputs, and long-run artifacts should be stored in
timestamped subdirectories here. The author scripts can take hours for full
training, so each run directory should keep stdout/stderr, command line,
environment notes, and any resulting figures or model weights.
